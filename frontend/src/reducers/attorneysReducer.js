const initialState = {
    attorneys: [],
}

const attorneysReducer = (state=initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default attorneysReducer;
const initialState = {
    attorneys: [],
    loading: true
}

const attorneysReducer = (state, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default attorneysReducer;